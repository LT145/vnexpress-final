{
    "buildCommand": "npm run vercel-build",
    "installCommand": "npm install",
    "framework": "nextjs",
    "regions": ["iad1"],
    "hooks": {
        "postDeploy": "npx prisma migrate deploy"
    }
}